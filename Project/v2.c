/*

module(displayResultsForEachClasses;char realClasses[], char estimatedClasses[];)

module(displayAccuracy;char realClasses[], char estimatedClasses[];)

module(displayBarCharts;char realClasses[], char estimatedClasses[];)



module(researchClasses;char realClasses[], char estimatedClasses[], int size;Classe** classes, int* nbClasses, int* sumNbGoodEstimation, int* sumTotal, int* maxTotal)

module(displayLegende;;)

module(displayXaxis;double maxValue, double ratio;)

module(displayBar;double value, double ratio;)

module(addOrUpdateClasses;Classe** classes, char real, char estimated;Classe** classes, int* nbClasses, int* sumNbGoodEstimation, int* sumTotal, int* maxTotal)

module(updateClasse;Classe* pClasse, bool isCorrect;)

module(addClasse;char real, bool isCorrectEstimation;Classe** classes, Classe* pPrecClasse, int* nbClasses, int* maxTotal)

module(createClasse;char name, bool isCorrectEstimation;Classe classe)

module(updateMaxTotal;int total;int* maxTotal)

module(updateSumTotalAndSumNbGoodEstimation;bool isCorrectEstimation;int* sumTotal, int* sumNbGoodEstimation)

module(calculatePourcentage;double numerator, double denominator;)

*/