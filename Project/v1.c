/*

module(displayResultsForEachClasses;realClasses, estimatedClasses;)

module(displayAccuracy;realClasses, estimatedClasses;)

module(displayBarCharts;realClasses, estimatedClasses;)



module(researchClasses;realClasses, estimatedClasses, size;classes, nbClasses, sumNbGoodEstimation, sumTotal, maxTotal)

module(displayLegende;;)

module(displayXaxis;maxValue, ratio;)

module(displayBar;value, ratio;)

module(addOrUpdateClasses;classes, real, estimated;classes, nbClasses, sumNbGoodEstimation, sumTotal, maxTotal)

module(updateClasse;pClasse, isCorrect;)

module(addClasse;real, isCorrectEstimation;classes, pPrecClasse, nbClasses, maxTotal)

module(createClasse;name, isCorrectEstimation;classe)

module(updateMaxTotal;total;maxTotal)

module(updateSumTotalAndSumNbGoodEstimation;isCorrectEstimation;sumTotal, sumNbGoodEstimation)

module(calculatePourcentage;numerator, denominator;)

*/